import React, { useContext } from 'react';

import './Header.css'

const Header = () => {
  
    return (
        <div class='header'>
            <h1>Team Detail</h1>
        </div>
    );
};

export default Header;